import XCTest
@testable import ios_twitter_text_counter

final class ios_twitter_text_counterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ios_twitter_text_counter().text, "Hello, World!")
    }
}
